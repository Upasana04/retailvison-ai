Multi-camera support is built into the architecture. 
The current implementation was validated on multiple CCTV feeds from the same retail store. 
Camera-specific zone calibration is supported through configurable zone mappings.
Due to differing camera perspectives, analytics such as dwell time, heatmaps, and funnel metrics are primarily generated from the main store-floor camera, while entrance/exit cameras contribute visitor entry and exit events.
